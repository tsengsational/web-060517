function enoughDonuts(numberOfDonuts){
  // numberOfDonuts = 3
  // let foo =
  const friends = ['bob', ]
  var numPeople = prompt('how many people are you bringing to the party')
  if(numPeople > numberOfDonuts){
    var foo = 'bar'
    return false
  } else {
    return true
  }
}

if(enoughDonuts(3)){
  alert.log('you have enough donuts')
} else {
  alert.log('not enough donuts')
}

// write some code with a partner
  // how many friends do you have coming to the party
  // less of number of donuts
  // you have enough donuts!!
  // if its more then
  // you have run out of donuts!!
