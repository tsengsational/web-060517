<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>
<ul>
  <% @users.each do |u| %>
  <li>
    <%= link_to(u.name, user_path(u)) %>
    <%= u.email %>
    <%= u.password_digest %>
  </li>
  <% end %>
</ul>
